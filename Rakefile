namespace :test do
  desc "Runs validation test on valid and invalid inputs"
  task :validation do
    ruby "tests/test_validation.rb"
  end
  
  desc "Runs test on any user input"
  task :input do
    ruby "tests/test_input.rb"
  end
end
